const ROUTES = {
  HOME: '/',
  LOGIN: '/login',
  LOGOUT: '/logout',
  REGISTER: '/register',
  BACKEND: {
    LOGIN: '/auth/login',
    REGISTER: '/auth/register'
  },
  HANDLER: {
    LOGIN: '/api/auth/login',
    REGISTER: '/api/auth/register'
  }
}

export default ROUTES
